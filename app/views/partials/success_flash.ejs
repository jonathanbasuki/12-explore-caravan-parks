<% if (success_msg) { %>
    <div id="flash-message" class="p-4 bg-green-500/10 text-green-100 border border-green-500/30 rounded text-sm">
        <p class="mt-1 text-sm">
            <%= success_msg %>
        </p>
    </div>
    <% } %>